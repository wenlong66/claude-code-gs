---
paths:
  - "src/ui/**"
---

# UI 代码规则

- UI 绝对不能拥有或直接修改游戏状态——只显示，使用命令/事件请求更改
- 所有 UI 文本必须通过本地化系统——不要硬编码面向用户的字符串
- 所有交互元素同时支持键盘/鼠标和游戏手柄输入
- 所有动画必须可跳过，并尊重用户的运动/无障碍偏好
- UI 声音通过音频事件系统触发，而不是直接触发
- UI 绝对不能阻塞游戏线程
- 可缩放文本和色盲模式是必需的，不是可选的
- 在最小和最大支持的分辨率下测试所有屏幕
