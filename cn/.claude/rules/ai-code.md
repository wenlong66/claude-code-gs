---
paths:
  - "src/ai/**"
---

# AI 代码规则

- AI 更新预算：每帧最多 2ms——进行分析验证
- 所有 AI 参数必须可从数据文件调整（行为树权重、感知范围、定时器）
- AI 必须可调试：为所有 AI 状态实现可视化钩子（路径、感知锥、决策树）
- AI 必须传达意图——玩家需要时间阅读和反应
- 优先使用基于实用性的或行为树方法，而不是硬编码的 if/else 链
- 群体 AI 必须支持阵型、侧翼和角色分配（从数据）
- 所有 AI 状态机必须记录转换以便调试
- 永远不要在没有验证的情况下信任网络传来的 AI 输入
